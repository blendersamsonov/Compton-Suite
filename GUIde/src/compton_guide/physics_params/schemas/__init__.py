"""Per-model ``ModelSpec``s. See ``schemas/kascade.py``.

``schemas/xigma.py`` moved to that model's own repo as ``xigma_i.params``
(``XIGMA_SPEC``/``XIGMA_DIAGNOSTIC_SPEC``) -- ``kascade`` hasn't had the
same schema-ownership move yet, so its spec still lives here."""
